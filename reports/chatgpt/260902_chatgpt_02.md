# Roll out Git synchronization, fast verification, security-tool, and environment policy

Task ID: 260902_chatgpt_02
Issued: 2026-09-02
Repository: cigit-zgy/agent-collaboration
Local repository: /Users/wenv/Documents/skills/agent-collaboration
Branch: master
Baseline SHA: 4e1cfdc4531bfbfb2aa9c07351739a5343297149
Verification level: LEVEL 1 — FOCUSED
Status: issued

## Mission

Roll the newly committed global collaboration policies into the machine-wide Codex
entry point, verify that Codex discovers them, audit availability of the selected
security tools without installing anything, and demonstrate the repository
synchronization protocol on this task itself.

The objective is a small machine-level rollout, not another redesign or broad
verification campaign.

## Current evidence / diagnosed problem

The `agent-collaboration` repository now owns explicit policies for:

- repository start/end synchronization;
- focused-development speed and verification levels;
- Semgrep/CodeQL/pip-audit selection;
- Skill/project environment ownership and clean-environment limits.

These durable decisions are recorded in:

- `reports/concept/00_global_codex_policy.md`;
- `references/protocol.md`;
- `references/verification-levels.md`;
- `references/verification-tools.md`;
- `references/skill-environment-policy.md`.

The current `/Users/wenv/.codex/AGENTS.md` predates these additions. It already
contains the correct overall global policy and must remain concise rather than
copying the detailed references.

## Authoritative sources

- `SKILL.md`
- `references/protocol.md`
- `references/verification-levels.md`
- `references/verification-tools.md`
- `references/skill-environment-policy.md`
- `reports/concept/00_global_codex_policy.md`
- `/Users/wenv/.codex/AGENTS.md`

## Scope

1. Synchronize the local `agent-collaboration` repository safely to this committed
   task before editing.
2. Update `/Users/wenv/.codex/AGENTS.md` with the exact concise additions below.
3. Verify the global AGENTS file is discovered in a clean/new Codex context.
4. Audit whether `semgrep`, `codeql`, and `pip-audit` are currently available;
   report versions/status only. Do not install them in this task.
5. Write `reports/codex/260902_codex_02.md`, commit, push, fetch again, and verify
   final `HEAD == upstream`.

## Required changes

### A. Repository synchronization before editing

Follow `references/protocol.md` exactly:

```text
git fetch
→ inspect branch / HEAD / upstream / git status --short
→ preserve any pre-existing user changes
→ safe fast-forward only when the branch is merely behind
→ no invented merge/rebase/reset/stash/force-push
→ verify task source and baseline consistency
```

If synchronization is unsafe because of divergence or conflicting user changes,
do not overwrite anything. Report the concrete state.

### B. Update `/Users/wenv/.codex/AGENTS.md`

Preserve the existing approved structure and substantive rules. Make only these
machine-wide additions/clarifications.

Under `## ChatGPT ↔ Codex collaboration`, after the existing acceptance sentence,
add this paragraph exactly:

```markdown
For formal repository tasks, Codex starts with `git fetch` plus explicit inspection of branch, `HEAD`, upstream, and worktree state; it uses only safe fast-forward synchronization and preserves pre-existing user changes. Repository-changing work ends with task-scoped commit + push + a fresh fetch and verification that local `HEAD` equals the tracked upstream. Detailed synchronization rules live in `/Users/wenv/Documents/skills/agent-collaboration/references/protocol.md`.
```

Replace the current `## Verification` body with exactly:

```markdown
## Verification

For formal ChatGPT → Codex work, use the verification level specified by the committed task in `agent-collaboration`. `LEVEL 1 — FOCUSED` is the ordinary development default: use directly affected tests and the smallest useful smoke/integration check, and optimize for fast feedback.

Do not upgrade a focused task into full-repository testing, clean-environment rebuilding, multiple-reviewer ceremony, release-grade verification, or security scanning unless the task/risk requires it. Detailed levels live in `/Users/wenv/Documents/skills/agent-collaboration/references/verification-levels.md`.

Security/static/dependency tools are selected by risk rather than run by default. The global choices are Semgrep for fast targeted scanning, CodeQL for deeper semantic/data-flow release or security gates, and `pip-audit` for relevant Python dependency audits. Detailed selection rules live in `/Users/wenv/Documents/skills/agent-collaboration/references/verification-tools.md`.

Tests passing does not by itself establish task acceptance.
```

Replace the current `## Environment and scope safety` body with exactly:

```markdown
## Environment and scope safety

Do not create or mutate project environments, dependencies, remotes, release state, or unrelated files unless the active task or project contract requires it. Documentation-only Skills own no environment; project-executed code uses the project's declared environment. Reuse a healthy environment and do not rebuild one merely to prove cleanliness. Detailed environment rules live in `/Users/wenv/Documents/skills/agent-collaboration/references/skill-environment-policy.md`.

For committed formal tasks, do not expand scope from chat context. The committed task is the implementation specification.
```

Do not add more global rules than these. Do not copy the full reference documents
into AGENTS.md.

### C. Security-tool availability audit

Check only availability/version without installing or configuring:

```text
semgrep
codeql
pip-audit
```

Use direct commands such as `command -v` and version output where available. Record
`AVAILABLE <version>` or `NOT INSTALLED` in the Codex report.

Absence is not a task failure because these tools are conditional, not universal
runtime dependencies.

Do not install Homebrew packages, Python packages, GitHub extensions, CodeQL
bundles, workflows, or scanner configuration in this task.

### D. Environment discipline

Do not create, rebuild, move, delete, or replace any `.venv`, Conda environment, or
other project environment in this task. No executable code change requires one.

### E. Final Git synchronization

After writing the Codex report:

```text
commit task-scoped repository changes
→ push current branch
→ git fetch
→ verify local HEAD == tracked upstream SHA
→ verify/report final worktree state
```

The machine-global AGENTS file is outside this Git repository and is therefore
verified by content/discovery, not committed into this repository.

## Non-goals

- No redesign of `agent-collaboration`.
- No third-party Skill migration or cleanup.
- No cleanup of the pre-existing duplicate `openai-docs` discovery.
- No installation/configuration of Semgrep, CodeQL, or pip-audit.
- No environment creation or clean-environment rebuild.
- No full repository test suite.
- No second Agent/reviewer unless a concrete unexpected risk requires one.
- No changes to `LOCAL_SKILLS.md` unless this task itself exposes a directly
  incorrect active path caused by the prior rollout.

## Scientific / product boundaries

This task concerns global development infrastructure only. It must not introduce
project-specific scientific or product semantics.

## Engineering constraints

Use Ponytail `full`: smallest sufficient changes, no new scripts, manifests,
state files, caches, hash registries, environment managers, or scanner wrappers.

Do not add persistent hashes. No new trust boundary in this task requires one.

Speed is a requirement: use focused checks only.

## Verification

LEVEL 1 — FOCUSED.

Run only:

1. content comparison confirming the required AGENTS blocks are exact and the rest
   of the approved file is not materially changed;
2. one clean/new Codex discovery smoke proving the global AGENTS file is loaded;
3. `command -v`/version checks for Semgrep, CodeQL, and pip-audit;
4. `git diff --check` for `agent-collaboration`;
5. final Git synchronization evidence (`HEAD == upstream`);
6. confirmation that this task created/rebuilt no environment.

Do not run pytest, Ruff, mypy, CodeQL, Semgrep scans, pip-audit scans, or release
checks: this task changes documentation/configuration only.

## Acceptance criteria

- Local `agent-collaboration` was synchronized safely before implementation.
- `/Users/wenv/.codex/AGENTS.md` contains exactly the specified concise policy
  additions/replacements.
- A clean/new Codex context demonstrates global AGENTS discovery.
- Semgrep, CodeQL, and pip-audit availability is reported truthfully without
  installation.
- No environment was created/rebuilt/moved/deleted by this task.
- `reports/codex/260902_codex_02.md` records the required Git start/end state.
- Task-scoped repository changes are committed and pushed.
- After final fetch, local `HEAD == upstream`.

## Git requirements

Follow `references/protocol.md` repository synchronization. Initial and final Git
state must be recorded using `references/codex-report-template.md`.

Do not force-push, reset hard, auto-stash user work, or create an implicit merge or
rebase.

## Codex report

Expected path:

`reports/codex/260902_codex_02.md`

Use `references/codex-report-template.md`. Additionally include a compact tool
availability table for Semgrep, CodeQL, and pip-audit and state explicitly whether
any environment was mutated.

## Final stdout

```text
VERDICT:
Task ID: 260902_chatgpt_02
Task source SHA:
Initial Git synchronization: PASS|FAIL
Global AGENTS update: PASS|FAIL
Global AGENTS discovery: PASS|FAIL
Semgrep: AVAILABLE <version>|NOT INSTALLED
CodeQL: AVAILABLE <version>|NOT INSTALLED
pip-audit: AVAILABLE <version>|NOT INSTALLED
Environment mutation: NONE|<explain>
Focused verification: PASS|FAIL
Final HEAD == upstream: PASS|FAIL
Codex report:
Final agent-collaboration SHA:
Remaining limitations:
```
