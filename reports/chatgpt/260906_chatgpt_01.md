---
artifact_type: chatgpt_task
task_id: 260906_chatgpt_01
title: Apply repository visibility cleanup and Actions verification policy
status: issued
date: 2026-09-06
repository: cigit-zgy/agent-collaboration
task_branch: task/260906-repository-visibility-actions-policy
baseline_sha: 66469c4ad4b8d7121497894eac0fb70bf8f0b756
verification_level: level_2
collaboration_commit: 66469c4ad4b8d7121497894eac0fb70bf8f0b756
coding_skill_profile: >
  cigit-zgy/agent-collaboration@66469c4ad4b8d7121497894eac0fb70bf8f0b756:
  references/collaboration/shared-coding-skills.md
summary: >
  Execute the User-authorized GitHub repository administration changes that are
  unavailable to ChatGPT's connected GitHub tool, after fail-closed publication
  safety checks, and verify that private-repository GitHub Actions now follow the
  collaboration claim-dedup and budget-placement standard.
codex_report: reports/codex/260906_codex_01.md
---

# Apply repository visibility cleanup and Actions verification policy

## Mission

Complete the repository-administration portion of the User's accepted GitHub Actions policy.

The User has explicitly decided:

```text
cigit-zgy/web-of-science-export
→ delete the GitHub repository

cigit-zgy/wos-spider
→ change PRIVATE → PUBLIC

cigit-zgy/paper_download
→ change PRIVATE → PUBLIC
```

ChatGPT has already updated the collaboration development standard and directly narrowed the known automatic Actions in the remaining private repositories where connected repository writes were sufficient.

This task performs only the local/admin work ChatGPT cannot execute through the connected GitHub administration surface, plus independent verification of the resulting repository/Actions state.

## Authority and frozen semantics

Collaboration authority:

- `cigit-zgy/agent-collaboration@66469c4ad4b8d7121497894eac0fb70bf8f0b756:SKILL.md`
- `cigit-zgy/agent-collaboration@66469c4ad4b8d7121497894eac0fb70bf8f0b756:references/collaboration/protocol.md`
- `cigit-zgy/agent-collaboration@66469c4ad4b8d7121497894eac0fb70bf8f0b756:references/collaboration/implementation.md`
- `cigit-zgy/agent-collaboration@66469c4ad4b8d7121497894eac0fb70bf8f0b756:references/collaboration/verification.md`
- `cigit-zgy/agent-collaboration@66469c4ad4b8d7121497894eac0fb70bf8f0b756:references/collaboration/shared-coding-skills.md`
- `cigit-zgy/agent-collaboration@66469c4ad4b8d7121497894eac0fb70bf8f0b756:references/collaboration/templates/codex-report.md`

Frozen User decisions:

1. The remote GitHub repository `cigit-zgy/web-of-science-export` is to be deleted.
2. `cigit-zgy/wos-spider` is intended to become PUBLIC.
3. `cigit-zgy/paper_download` is intended to become PUBLIC.
4. Public-repository standard GitHub-hosted CI may remain when useful; do not remove it merely because Codex can also run the commands.
5. Private repositories default to Codex/local verification and no automatic push/PR Actions unless a distinct hosted claim justifies them.
6. The same verification claim should not be repeated in Codex and GitHub Actions without a distinct environment/status/release reason.
7. Repository publication must remain fail-closed for secrets/private data: an exposure blocker may stop a visibility change even though PUBLIC is the intended final state.

Do not reinterpret these decisions into unrelated repository cleanup, open-source relicensing, history rewriting, or source deletion.

## Shared coding Skills and authoring state

Shared profile:

- `cigit-zgy/agent-collaboration@66469c4ad4b8d7121497894eac0fb70bf8f0b756:references/collaboration/shared-coding-skills.md`

Activated shared Skills:

- `ponytail` — mode `full`; minimize administration and verification machinery, but never weaken publication/deletion safety.

Additional project/task Skill authorities:

- none.

ChatGPT-authored before handoff:

### agent-collaboration

Current master contains the new Actions standard:

- `references/collaboration/verification.md` — claim deduplication, private/public defaults, trigger/matrix guidance;
- `references/collaboration/implementation.md` — Codex/Actions duplicate-claim prohibition;
- `SKILL.md` and `README.md` — routing;
- `reports/concept/10_github_actions_budget_and_verification_placement.md` — decision history.

### water-biomodel-agent

ChatGPT directly changed:

- `.github/workflows/ci.yml`
  - commit `62b5c30ff940c4912fee798a8a71706b742ba8bd`
  - now manual `workflow_dispatch` only;
  - one Ubuntu/Python 3.12 clean-room milestone job instead of an automatic three-environment matrix.

- `.github/workflows/first-two-skills-release.yml`
  - commit `18b5cb7392b18dc0d4acb3f1868aad24ee764178`
  - now manual `workflow_dispatch` only;
  - retains the three-environment matrix as a deliberate release qualification gate.

### asm-chinese

ChatGPT directly changed:

- `.github/workflows/build.yml`
  - commit `2af35b8c2885155383b75f2ab08e5588d83cefa6`
  - now manual `workflow_dispatch` only;
  - retains the independent Linux/TeX clean-build capability.

- `.github/workflows/diagnose-wide-tables.yml`
  - removed at commit `c073f7391b941574baa4ede968c21f8cb209ef28`;
  - it only printed source context and supplied no distinct GitHub-hosted claim beyond local Codex capability.

ChatGPT checks already completed:

- repository metadata confirmed `water-biomodel-agent`, `wos-spider`, `asm-chinese`, `paper_download`, `web-of-science-export`, `wastewater-model-evolution`, `structure-object-perspective`, and `tikz-skill` were PRIVATE before this policy application;
- `wos-spider` currently owns `ci.yml` and `release.yml` and is intended to become PUBLIC, so this task does not disable those workflows merely for budget reasons;
- `water-biomodel-agent` and `asm-chinese` automatic workflow changes above were committed remotely.

Remaining Codex-local/admin work:

- establish actual local GitHub CLI authorization/capability;
- audit `wos-spider` and `paper_download` before public exposure;
- make both repositories PUBLIC when safe;
- delete `web-of-science-export` remotely;
- verify the ChatGPT-authored private-workflow changes and remaining private-repository Actions state;
- publish the task report.

## LOCAL/admin scope

This task may use:

- authenticated `gh` CLI and GitHub API calls available under the User's existing credentials;
- existing local checkouts/caches for the named repositories when present;
- temporary scratch only under `/Users/wenv/Documents/skills/agent-collaboration/tmp/260906_chatgpt_01/` if needed;
- read-only clones under that task tmp boundary only when an exact full-history audit cannot be performed from an existing clean checkout.

Do not create sibling worktrees or scratch directories outside the project `tmp/` boundary.

Do not alter authentication configuration or request/refresh new OAuth scopes interactively without reporting that requirement as a blocker. Use the authorization already present.

## Required execution

### 1. Bind the task and Git state

In the canonical local `agent-collaboration` checkout:

```text
fetch
→ inspect branch / HEAD / upstream / worktree
→ preserve User state
→ safely reach task/260906-repository-visibility-actions-policy at exact handoff commit
→ verify pinned collaboration authority
→ verify activated Ponytail authority
```

A clean same-checkout task branch is sufficient. Do not create a linked worktree unless the canonical checkout cannot safely execute the task; any needed worktree belongs under the task tmp boundary.

### 2. Establish GitHub administration capability

Run read-only checks first:

```text
gh auth status
current authenticated account
repository metadata/visibility for all three target repositories
```

Determine whether the existing credentials can:

- edit repository visibility;
- delete a repository.

Do not mutate auth configuration merely to make the task pass.

If deletion or visibility administration is unavailable under current credentials, complete all independent safe work and report the exact missing capability/scope as BLOCKED for that action.

### 3. Publication-safety audit — wos-spider

Before changing visibility, establish the exact remote repository and inspect enough of the **full reachable Git history**, not only the working tree, to rule out obvious public-exposure blockers.

At minimum inspect for:

```text
API keys / access tokens / passwords / session cookies
private URLs or credentials
personal identifiers or private account data
proprietary/private datasets or exports
browser/session artifacts
large/generated files that were never intended for public release
third-party source/content that clearly cannot be redistributed publicly
```

Use an already-installed secret scanner if available and useful, but do not install a new framework solely for ceremony. If no scanner is available, perform deterministic full-history search plus targeted manual inspection. The repository is small enough that current-tree-only inspection is insufficient.

Also inspect:

```text
README / project description
LICENSE or explicit absence of license
.gitignore
workflow files
release metadata
```

Important boundary:

- absence of a LICENSE alone does not block making a repository PUBLIC; PUBLIC and open-source licensing are separate decisions;
- a real secret/private-data/redistribution blocker DOES block publication;
- do not rewrite history or delete evidence to force publication inside this task.

Record the audit evidence and conclusion in the report.

### 4. Publication-safety audit — paper_download

Apply the same full-history/public-exposure audit to `cigit-zgy/paper_download`.

Pay particular attention to publisher/authentication/session artifacts, downloaded copyrighted full-text material, API keys, cookies, credentials, or source files that should not be redistributed in a public repository.

If a blocker exists, preserve PRIVATE visibility and report the exact blocker. Do not sanitize/rewrite history in this task.

### 5. Make approved repositories PUBLIC

Only after the corresponding audit passes:

```text
cigit-zgy/wos-spider
→ PUBLIC

cigit-zgy/paper_download
→ PUBLIC
```

Using `gh`, use an explicit visibility-changing command that acknowledges GitHub's visibility-change consequence guard.

After each change, independently re-read repository metadata and verify `visibility == PUBLIC`.

Do not change repository names, default branches, topics, descriptions, release state, Actions files, or local remotes merely because visibility changed.

### 6. Delete web-of-science-export remote repository

The User explicitly authorized remote deletion of:

```text
cigit-zgy/web-of-science-export
```

Before deletion record:

```text
repository full name
visibility
default branch
current default-branch HEAD when resolvable
remote URL
```

Then delete the **remote GitHub repository** using the existing authenticated GitHub CLI capability.

Do not delete any local directory or local Git history for this repository in this task.

After deletion, verify the remote repository no longer resolves through `gh`/GitHub API.

If current auth lacks repository-delete permission, do not work around it by changing credentials; report the exact blocker.

### 7. Verify private-repository Actions policy after ChatGPT direct edits

Verify current remote state of:

#### water-biomodel-agent

Expected:

```text
visibility: PRIVATE
.github/workflows/ci.yml
→ workflow_dispatch only
→ single Ubuntu/Python 3.12 clean-room job

.github/workflows/first-two-skills-release.yml
→ workflow_dispatch only
→ three-environment release matrix retained

no automatic push/pull_request trigger remains in those workflows
```

#### asm-chinese

Expected:

```text
visibility: PRIVATE
.github/workflows/build.yml
→ workflow_dispatch only

.github/workflows/diagnose-wide-tables.yml
→ absent
```

Do not rerun these workflows merely to prove their YAML changed.

### 8. Audit remaining private repositories for automatic Actions

After any visibility changes, enumerate the User-owned repositories that remain PRIVATE.

For each remaining private repository, inspect `.github/workflows/` when present and classify any automatic `push`, `pull_request`, `schedule`, or similar trigger as:

```text
DISTINCT_HOSTED_CLAIM
→ release/deploy/status/clean-room/matrix property not already covered locally

LIKELY_REDUNDANT
→ ordinary lint/test/build claim already owned by normal Codex verification

NONE
→ no automatic Actions
```

This step is an audit only for repositories other than `water-biomodel-agent` and `asm-chinese`. Do not modify additional repositories in this task. Report any unexpected automatic private workflow for later User + ChatGPT adjudication.

### 9. Fresh evidence and task report

Write exactly:

```text
reports/codex/260906_codex_01.md
```

The report must include:

1. GitHub auth/admin capability actually available;
2. before/after visibility table for `wos-spider` and `paper_download`;
3. publication-safety evidence and conclusion for each;
4. deletion result for `web-of-science-export` plus recorded pre-delete coordinates;
5. verification of ChatGPT-authored Actions changes in `water-biomodel-agent` and `asm-chinese`;
6. audit table for all remaining private repositories and automatic Actions classification;
7. any blockers/limitations;
8. task tmp disposition;
9. final task-branch Git state.

Commit/push the report to `task/260906-repository-visibility-actions-policy`, fetch again, and verify task-branch local HEAD equals upstream.

Clean task tmp state when no recovery value remains.

## Non-goals

- No history rewrite/sanitization of `wos-spider` or `paper_download`.
- No automatic relicensing or LICENSE creation.
- No deletion of local `web-of-science-export` files.
- No renaming repositories or default branches.
- No modification of public `wos-spider` workflows merely because the commands duplicate local commands; public reproducibility/status is an accepted distinct property unless separately redesigned.
- No workflow execution merely to consume/test Actions minutes.
- No change to scientific/project implementation.
- No plugin/dependency/environment install or update unless already required by an existing repository contract; secret-audit tooling should use what is already available or direct Git inspection.
- No broad repository cleanup unrelated to visibility/Actions policy.
- No interactive credential escalation hidden inside the task.

## Engineering and safety constraints

- Use `ponytail full`: smallest sufficient administration path.
- Publication is fail-closed on real secret/private-data/redistribution ambiguity.
- Repository deletion is allowed only for the exact User-authorized full name `cigit-zgy/web-of-science-export`.
- Do not infer permission to delete forks, local clones, branches, tags, releases, or similarly named repositories.
- Visibility changes apply only to the exact two named repositories.
- Preserve all unrelated repository state.
- Do not treat GitHub stars/popularity or PUBLIC visibility as a scientific-quality claim.

## Acceptance criteria

A full PASS requires:

- `cigit-zgy/wos-spider` is PUBLIC and its pre-publication audit found no blocking exposure issue;
- `cigit-zgy/paper_download` is PUBLIC and its pre-publication audit found no blocking exposure issue;
- `cigit-zgy/web-of-science-export` remote repository is deleted and verified absent;
- `water-biomodel-agent` private workflows match the manual clean-room/release design above;
- `asm-chinese` private workflow state matches the manual-build/no-diagnostic design above;
- remaining private repositories have an Actions audit with any unexpected automatic workflow surfaced;
- no unrelated repository/content/auth/environment state was changed;
- report exists at the bound path and task branch is pushed/synchronized.

If a publication audit finds a real blocker, preserve the affected repository PRIVATE and use `PASS_WITH_LIMITATIONS` only if every other requested action is complete and the blocker is an external/User decision rather than unfinished machine-solvable work. Use `BLOCKED` when required GitHub administration cannot be performed with current authorization or a critical publication decision is unresolved.

## Verification

Verification level: LEVEL 2

ChatGPT checks already completed:

- collaboration policy projection: PASS;
- known private Actions workflow rewrites/removal: committed remotely;
- target repository identities/initial visibility: resolved.

Remaining Codex-local/admin evidence:

- Security/publication boundary: full-history exposure audit for both publication targets.
- External/admin result: visibility changes and remote deletion through authenticated GitHub CLI.
- Contract/invariant: exact repository identities only; no unrelated admin mutation.
- Actions placement: named private workflows match current policy; remaining private-repo automatic Actions inventoried.
- Git result: report committed/pushed and task branch synchronized.

Shared coding-Skill alignment:

- verify `ponytail` against the task-pinned shared profile before administration work.

## Git handoff / integration

Task branch: `task/260906-repository-visibility-actions-policy`

Target integration branch: `master`

Post-acceptance integration: `AUTO`

The repository report integration is mechanical after acceptance. The visibility/deletion actions themselves are already explicitly authorized by the User and are executed during this task subject to the fail-closed publication checks above.

## Codex report

```text
reports/codex/260906_codex_01.md
```
