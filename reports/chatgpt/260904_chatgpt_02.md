---
artifact_type: chatgpt_task
task_id: 260904_chatgpt_02
title: Clean legacy local Agent test state and bind global collaboration refresh
status: issued
date: 2026-09-04
repository: cigit-zgy/agent-collaboration
task_branch: task/260904-local-workspace-cleanup
baseline_sha: b6dea0ad9a676bdc9901fa365a260345573eaf5b
verification_level: level_2
collaboration_commit: b6dea0ad9a676bdc9901fa365a260345573eaf5b
coding_skill_profile: >
  cigit-zgy/agent-collaboration@b6dea0ad9a676bdc9901fa365a260345573eaf5b:
  references/collaboration/shared-coding-skills.md
summary: >
  Remove obsolete Agent-created sibling worktrees/test material under the User's
  skills root, reconcile machine-wide Codex guidance with the new collaboration
  authority-refresh and project-local tmp hard boundaries, and leave no active
  test scratch state because the User explicitly states no tests are currently running.
codex_report: reports/codex/260904_codex_02.md
---

# Clean legacy local Agent test state and bind global collaboration refresh

## Mission

Perform one explicit local housekeeping pass after the collaboration policy was tightened.

The User has explicitly stated that no current tests are running and that the existing Agent-created test/worktree material shown under `/Users/wenv/Documents/skills/` may be deleted. This is the human authorization for removing obsolete Agent-created test state in the scope below.

The task must:

1. remove obsolete sibling linked worktrees and other clearly Agent-created test/scratch directories for `agent-collaboration` and `wos-spider` under `/Users/wenv/Documents/skills/`;
2. remove obsolete local `tmp/` test/scratch contents for those two canonical projects, while using the new project-local `tmp/` boundary for this task itself;
3. update `/Users/wenv/.codex/AGENTS.md` with concise hard rules for collaboration authority refresh and project-local temporary state;
4. safely refresh the local `agent-collaboration` cache/checkout when it is a clean fast-forward case, without making local cache freshness an authority requirement;
5. verify a fresh Codex context recovers the new global rules;
6. publish one concise Codex report at the bound path.

## Authority and frozen semantics

Project/collaboration authority:

- `cigit-zgy/agent-collaboration@b6dea0ad9a676bdc9901fa365a260345573eaf5b:AGENTS.md`
- `cigit-zgy/agent-collaboration@b6dea0ad9a676bdc9901fa365a260345573eaf5b:SKILL.md`
- `cigit-zgy/agent-collaboration@b6dea0ad9a676bdc9901fa365a260345573eaf5b:references/collaboration/protocol.md`
- `cigit-zgy/agent-collaboration@b6dea0ad9a676bdc9901fa365a260345573eaf5b:references/collaboration/shared-coding-skills.md`
- `cigit-zgy/agent-collaboration@b6dea0ad9a676bdc9901fa365a260345573eaf5b:references/project/architecture.md`
- `cigit-zgy/agent-collaboration@b6dea0ad9a676bdc9901fa365a260345573eaf5b:references/collaboration/templates/codex-report.md`

Frozen semantics:

- Agent-created local temporary state belongs under `<PROJECT_ROOT>/tmp/<WORK_ID>/` unless the User explicitly authorizes another location;
- FORMAL linked worktrees belong under `<PROJECT_ROOT>/tmp/<TASK_ID>/worktree/` by default;
- new sibling worktrees such as `../<project>-<sha>/` are prohibited as normal task scratch state;
- cleanup is part of completion; registered Git worktrees are removed with Git-aware commands;
- ChatGPT refreshes current collaboration authority once at the start of a new repository-changing work unit and FORMAL tasks pin it;
- FORMAL Codex work always uses the task-pinned collaboration commit;
- unpinned LOCAL-QUICK/repository-changing Codex work refreshes current collaboration authority once at the first execution step of a new Codex repository task/session and reuses it for that contiguous work unit;
- neither ChatGPT nor Codex re-fetches the same collaboration authority on every message merely for ceremony;
- a local/discovered `agent-collaboration` copy is a cache, not authority by filename/path alone.

## Shared coding Skills and authoring state

Shared profile:

- `cigit-zgy/agent-collaboration@b6dea0ad9a676bdc9901fa365a260345573eaf5b:references/collaboration/shared-coding-skills.md`

Activated shared Skills:

- `ponytail` — `full`; applies to housekeeping/minimal implementation decisions and must not weaken deletion-safety or authority boundaries.

Additional project/task Skill authorities:

- none.

ChatGPT-authored before handoff:

- all collaboration policy changes establishing the authority-refresh and project-local temporary-state hard boundaries;
- this formal task.

ChatGPT checks already completed:

- current remote collaboration `master` was refreshed before this work unit;
- current protocol/project architecture/task/report contracts contain the new refresh/tmp rules;
- task branch was created from the exact current policy baseline.

Remaining Codex-local work:

- inspect actual local worktree/test state;
- delete the User-authorized obsolete Agent-created test state;
- update the machine-wide Codex `AGENTS.md` concisely;
- run fresh local Codex recovery verification;
- publish the report.

## LOCAL scope

This task requires local-only access to:

- `/Users/wenv/Documents/skills/` directory contents;
- `/Users/wenv/Documents/skills/agent-collaboration` and its Git worktree registry;
- `/Users/wenv/Documents/skills/wos-spider` and its Git worktree registry;
- sibling directories beginning `agent-collaboration-` or `wos-spider-`;
- project-local `tmp/` contents for `agent-collaboration` and `wos-spider`;
- `/Users/wenv/.codex/AGENTS.md` and any override;
- installed Codex CLI startup behavior.

The task is destructive only with respect to the explicitly User-authorized obsolete Agent-created test/worktree state described below. Canonical project/source directories are not deletion targets.

## Required changes

### 1. Establish this task without creating another sibling worktree

Use the canonical repository:

```text
/Users/wenv/Documents/skills/agent-collaboration
```

Fetch and inspect branch/HEAD/upstream/worktree state. Safely reach the exact handoff commit.

If a dedicated worktree is needed, create it only at:

```text
/Users/wenv/Documents/skills/agent-collaboration/tmp/260904_chatgpt_02/worktree/
```

Do not create `/Users/wenv/Documents/skills/agent-collaboration-<...>` for this task.

Preserve unrelated User changes in the canonical checkout. Do not reset, auto-stash, force-push, or invent conflict reconciliation.

### 2. Inventory legacy `agent-collaboration` worktrees/test state

From the canonical `agent-collaboration` repository, run Git-aware worktree inventory first.

Inspect all non-primary linked worktrees and sibling directories under `/Users/wenv/Documents/skills/` whose names begin:

```text
agent-collaboration-
```

For every candidate, record before deletion when applicable:

```text
path
registered worktree: yes/no
HEAD
branch/detached state
status/dirty state
upstream/ahead/behind when available
relationship to canonical agent-collaboration Git common dir/origin
```

The User explicitly authorizes deletion of these old Agent-created test/worktree materials because no test is active. Therefore a dirty/unpushed state does not by itself block deletion **only after** the candidate is proven to be an Agent-created non-primary test/worktree belonging to `agent-collaboration` and the pre-delete state has been recorded in the report.

Do not delete the canonical directory:

```text
/Users/wenv/Documents/skills/agent-collaboration
```

Do not delete an ambiguous sibling directory that cannot be proven to be an obsolete Agent-created `agent-collaboration` test/worktree.

For registered linked worktrees, use `git worktree remove`; `--force` is authorized only for these proven legacy Agent-created test worktrees under the User's explicit deletion instruction. Then run `git worktree prune` as appropriate.

For an unregistered leftover sibling directory, filesystem deletion is authorized only after proving it is obsolete Agent-created scratch associated with the canonical repository and not an independent/canonical source.

### 3. Inventory and remove legacy `wos-spider` worktrees/test state

Apply the same procedure to:

```text
/Users/wenv/Documents/skills/wos-spider
/Users/wenv/Documents/skills/wos-spider-*
```

Record pre-delete Git state for every candidate. Remove all proven obsolete Agent-created non-primary test/worktrees because the User explicitly states no test is active and all such test material may be deleted.

Do not delete the canonical directory:

```text
/Users/wenv/Documents/skills/wos-spider
```

Do not delete an ambiguous sibling directory that cannot be proven to belong to obsolete Agent-created `wos-spider` testing.

Use Git-aware worktree removal/pruning for registered worktrees.

### 4. Clean project-local temporary state

Inspect:

```text
/Users/wenv/Documents/skills/agent-collaboration/tmp/
/Users/wenv/Documents/skills/wos-spider/tmp/
```

The User states no tests are active. Remove obsolete Agent-created test/scratch contents in these two `tmp/` trees.

Exception while this task is running:

```text
agent-collaboration/tmp/260904_chatgpt_02/
```

may exist only for this task and must itself be removed after the report is committed/pushed and the task no longer needs recovery state.

Do not delete project-owned durable artifacts outside `tmp/` merely because they look test-related.

### 5. Final top-level cleanliness check

After cleanup, inspect `/Users/wenv/Documents/skills/` and verify that no proven obsolete sibling test/worktree directory remains matching:

```text
agent-collaboration-*
wos-spider-*
```

Any preserved candidate must be named in the report with the exact reason it was not proven safe/in-scope to remove.

Do not clean unrelated project directories or unrelated worktrees in this task.

### 6. Machine-wide Codex `AGENTS.md` refresh contract

Inspect the current active machine-wide files:

```text
/Users/wenv/.codex/AGENTS.md
/Users/wenv/.codex/AGENTS.override.md  # if present
```

Preserve the existing global ownership/discovery/engineering/verification/environment/scope rules unless they conflict with current collaboration authority.

Make the smallest edit needed so the active machine-wide policy contains these durable rules in concise form:

```text
Collaboration refresh
- FORMAL task: use the exact task-pinned cigit-zgy/agent-collaboration commit.
- Unpinned LOCAL-QUICK or other repository-changing local task: at the first execution step of a new Codex repository task/session, resolve current cigit-zgy/agent-collaboration authority once and reuse it for that contiguous work unit.
- Do not rely on remembered policy or a stale local checkout; local discovery/checkout is a cache only unless exact repository+commit is verified.
- Do not re-read/re-fetch the same collaboration authority on every message merely for ceremony.

Local temporary-state boundary
- All Agent-created local scratch/test/worktree/download/render/cache/disposable-env state must remain under <PROJECT_ROOT>/tmp/<WORK_ID>/ unless the User explicitly authorizes another location.
- FORMAL linked worktree default: <PROJECT_ROOT>/tmp/<TASK_ID>/worktree/.
- Do not create sibling project worktrees or Desktop/Documents-root test folders for convenience.
- Clean completed temporary state when no recovery value remains; use git worktree remove/prune for linked worktrees.
```

Route detailed semantics to the current collaboration owners rather than copying the full protocol. Active collaboration paths must include current owners such as:

```text
references/collaboration/protocol.md
references/collaboration/implementation.md
references/collaboration/shared-coding-skills.md
references/collaboration/verification.md
references/project/architecture.md
references/skill/package.md
references/skill/repository.md
```

Remove active stale collaboration paths if any remain.

### 7. Safe local collaboration-cache refresh

Inspect the canonical local checkout:

```text
/Users/wenv/Documents/skills/agent-collaboration
```

After fetching, if its local `master` is clean and merely behind `origin/master`, fast-forward it safely to current remote `master` when this does not interfere with the active task worktree.

If it is dirty, divergent, on another branch, or otherwise unsafe to move, preserve it and report the state. Do not make task acceptance depend on mutating the cache; pinned/current GitHub authority remains the source of truth.

### 8. Fresh Codex verification

Start a fresh ephemeral/read-only Codex context after the global AGENTS edit.

Without relying on chat memory, verify startup can recover at least:

```text
FORMAL → task-pinned collaboration authority
unpinned repository-changing LOCAL work → one current-authority refresh per new task/session
no per-message ceremonial refresh
local checkout/discovery = cache, not authority by path alone
Agent-created local scratch = <PROJECT_ROOT>/tmp/<WORK_ID>/
FORMAL worktree = <PROJECT_ROOT>/tmp/<TASK_ID>/worktree/
no sibling project worktree as normal scratch
completed work tmp is cleaned when no recovery value remains
```

Do not run unrelated project tests, install dependencies, mutate plugins, or start external scientific services.

### 9. Report and self-cleanup

Write exactly:

```text
reports/codex/260904_codex_02.md
```

The report must contain:

- complete removed-path inventory;
- any preserved-path inventory with reason;
- before/after `git worktree list --porcelain` evidence for both canonical repositories;
- before/after top-level sibling-directory evidence;
- machine-wide AGENTS change summary;
- fresh Codex rule-recovery result;
- local canonical `agent-collaboration` cache disposition;
- this task's own temporary workspace disposition.

Commit/push the report on `task/260904-local-workspace-cleanup`, fetch again, and verify task-branch local HEAD equals upstream.

Then, when no recovery value remains, leave the task branch/report remote but remove this task's local worktree/scratch state under:

```text
/Users/wenv/Documents/skills/agent-collaboration/tmp/260904_chatgpt_02/
```

Perform self-cleanup from outside the linked worktree using Git-aware removal.

## Non-goals / ownership boundary

- Do not delete `/Users/wenv/Documents/skills/agent-collaboration`.
- Do not delete `/Users/wenv/Documents/skills/wos-spider`.
- Do not delete `00_archive`, `axiomfig`, `paper_download`, `sci-manuscript`, `tikz-skill`, `LOCAL_SKILLS.md`, or unrelated canonical sources.
- Do not clean unrelated project worktrees merely because they are old.
- Do not modify any scientific/project implementation.
- Do not alter WoS production data or resume production download.
- Do not install/update/remove plugins.
- Do not create/rebuild project Conda/venv environments.
- Do not update third-party Skills to upstream latest.
- Do not create another cleanup manifest/database/daemon/cron job.
- Do not create new sibling worktrees while fixing the sibling-worktree problem.

## Engineering constraints

- Use `ponytail full`: minimal operations and no new cleanup framework.
- Inspect before deletion and keep an evidence table in the report.
- Explicit User authorization covers deletion of **proven Agent-created obsolete test/worktree state** in the named scope, including dirty/unpushed temporary state after it is recorded.
- Ambiguous or potentially canonical/User-authored state remains fail-closed and is preserved.
- Registered worktrees use Git-aware removal; do not blindly `rm -rf` them.
- No new scripts are required unless a tiny one-off command materially reduces deletion error risk; do not commit such scratch helpers.
- Any scratch needed by this task itself remains inside the project-local task `tmp/` boundary.

## Acceptance criteria

- All proven obsolete `agent-collaboration-*` sibling Agent test/worktrees in scope are removed.
- All proven obsolete `wos-spider-*` sibling Agent test/worktrees in scope are removed.
- No canonical source directory is deleted or modified outside authorized machine-global guidance/cache operations.
- Obsolete `tmp/` test/scratch content in `agent-collaboration` and `wos-spider` is removed; current task scratch is self-cleaned after publication.
- Final Git worktree registries contain no stale removed entries for the cleaned candidates.
- `/Users/wenv/.codex/AGENTS.md` expresses the current one-refresh-per-work-unit collaboration rule and project-local tmp hard boundary without copying the whole collaboration manual.
- Active machine-wide collaboration paths are current; stale old-layout paths are absent.
- Fresh Codex startup recovers the intended refresh/tmp behavior.
- No unrelated project, plugin, dependency, environment, scientific data, or third-party Skill is changed.
- Codex report exists at the exact bound path and the task branch is pushed/synchronized.
- Final report truthfully distinguishes removed, preserved, and self-cleaned state.

## Verification

Verification level: LEVEL 2

ChatGPT checks already completed:

- current collaboration authority refreshed for this work unit;
- protocol/project architecture/task/report templates contain the new authority-refresh and tmp hard boundaries;
- task branch created from the exact current collaboration baseline.

Remaining Codex-local evidence:

- Destructive-scope proof: each deleted sibling candidate is proven to be an Agent-created non-primary test/worktree in the authorized `agent-collaboration`/`wos-spider` scope.
- Git/worktree integrity: before/after worktree registries and prune result for both repositories.
- Filesystem result: before/after sibling directory listing and project-local `tmp/` state.
- Global instruction contract: machine-wide AGENTS contains concise current refresh/tmp rules and no stale active collaboration path.
- Fresh host behavior: ephemeral Codex recovers the new rules without chat memory.
- Scope safety: canonical sources, unrelated projects, plugins, environments, dependencies, and scientific data unchanged.
- Git result: report committed/pushed; task branch synchronized; task-local worktree/scratch self-cleaned.

Shared coding-Skill alignment:

- verify `ponytail` against the task-pinned shared profile before local housekeeping decisions.

Temporary-state closure:

- all obsolete in-scope state and this task's own temporary workspace cleaned; any preserved ambiguous state named explicitly.

## Git handoff / integration

Task branch: `task/260904-local-workspace-cleanup`

Target integration branch: `master`

Post-acceptance integration: `AUTO`

Local temporary workspace:

```text
/Users/wenv/Documents/skills/agent-collaboration/tmp/260904_chatgpt_02/
```

Explicit User-authorized cleanup exception:

```text
legacy Agent-created test/worktree state under /Users/wenv/Documents/skills/
matching agent-collaboration-* or wos-spider-* and proven to belong to the two canonical repositories;
legacy obsolete test/scratch contents in the two canonical project tmp/ trees.
```

Do not merge `master` locally. ChatGPT will perform acceptance review and remote integration when safe.

## Codex report

```text
reports/codex/260904_codex_02.md
```
