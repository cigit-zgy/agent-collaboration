---
artifact_type: codex_report
task_id: AGENT_COLLABORATION_DISABLE_HUMAN_IN_LOOP_01
title: Pause human-in-loop integration
verdict: PASS
date: 2026-09-17
repository: cigit-zgy/agent-collaboration
task_branch: task/260917-disable-human-in-loop
task_source_sha: 995b1215f7555d16191a653474006052df7f8cca
baseline_sha: da6652021b04b20288a9fb70f0a6b3fa27126fce
verification_level: level_2
summary: >
  Removed machine-wide human-in-loop policy, MCP, routing, and Skill-discovery
  activation while preserving the canonical source checkout and paused wrapper
  source. The final active activation count is zero.
limitations: []
---

# Pause human-in-loop integration — Codex report

## Authority and repository state

The committed task at `995b1215f7555d16191a653474006052df7f8cca`
was the sole task-specific authority. Its only tree change from the pinned
collaboration baseline `da6652021b04b20288a9fb70f0a6b3fa27126fce` was the
task file. `SKILL.md` and `references/` are byte-identical across those two
revisions, so the active collaboration discovery symlink remains policy-content
aligned with the required revision.

Initial synchronization used a fresh fetch. The canonical checkout was clean at
the baseline, had one registered worktree, and switched to the fetched tracking
branch `task/260917-disable-human-in-loop` at the exact task commit. No merge,
rebase, reset, stash, force operation, or unrelated branch integration occurred.

## Active surfaces inspected and changes

Before cleanup, six Codex-facing activation surfaces were present:

1. `/Users/wenv/.codex/AGENTS.md` contained the mandatory machine-wide
   human-in-loop activation and managed AskHuman block.
2. `/Users/wenv/.codex/config.toml` registered
   `[mcp_servers.human-in-loop]` and its environment block.
3. The same config routed `mcp__askhuman` through
   `features.code_mode.direct_only_tool_namespaces`.
4. `/Users/wenv/.agents/skills/human-in-loop` was a copied discovery Skill.
5. `/Users/wenv/.agents/skills/ask-human` was a discovery symlink.
6. `/Users/wenv/.codex/skills/ask-human` remained in a runtime-scanned legacy
   discovery root.

The machine-wide AGENTS file now ends with the collaboration-native scope and
decision-boundary rules. The human-in-loop section, managed block, mandatory
`ask_human` workflow, and terminal `notify_human` requirement were removed.
The existing rule to stop at real scientific/product, destructive,
security/permission, public/external, or explicit User checkpoints remains.

The human-in-loop MCP registration, its environment block, and the
`mcp__askhuman` direct-only namespace route were removed from Codex config.
Unrelated MCP servers, plugins, features, and settings were not changed. The
remaining project trust entry for the preserved source checkout is not a server,
autoload, tool route, or Skill discovery entry.

Both user-discovery entries were removed. The copied `human-in-loop` Skill was
moved intact to the recoverable Trash path
`/Users/wenv/.Trash/human-in-loop-discovery-paused-260917`; it is outside every
Codex Skill discovery root. The `ask-human` symlink was removed, and its legacy
wrapper source was moved intact from the scanned `~/.codex/skills` directory to
the non-discovery rollback path
`/Users/wenv/.codex/upstream/paused/ask-human`. The CLI executable and external
service configuration were not uninstalled or modified.

`/Users/wenv/Documents/skills/LOCAL_SKILLS.md` now records both
`human-in-loop` and `ask-human` as `none (paused)`, retains their source and
revision provenance, and does not present either as a discovery authority.

## Preserved source evidence

The canonical source checkout `/Users/wenv/Documents/skills/human-in-loop`
remains present and unchanged:

- origin: `https://github.com/cigit-zgy/human-in-loop.git`
- branch: `master`
- HEAD: `3e4137dcd4f89950a9c284634e089ccecffcd0ce`
- tree: `8bbe5e7219f4e66bb176a0508d2863cc1c203d27`
- porcelain-status SHA-256 before/after:
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
- `SKILL.md` SHA-256 before/after:
  `1b1ffd105c13f44031921033043e9a1a58eccbc050d61acca05bbedb6d9c7c2b`

The checkout was already three commits behind its upstream; this task did not
fetch, update, run, or test it. The preserved rollback wrapper and copied Skill
also remain readable at their paused locations.

## Focused Level-2 verification

Fresh post-change checks established:

- case-insensitive search of machine-wide AGENTS finds no `human-in-loop`,
  `ask_human`, `notify_human`, or AskHuman directive;
- neither `$HOME/.agents/skills` nor `$HOME/.codex/skills` has a top-level
  human/loop discovery entry;
- parsed `mcp_servers` keys are only `computer-use`, `fetch`, `filesystem`,
  `git`, `node_repl`, and `openai-docs`;
- no human-in-loop/AskHuman MCP section or tool-routing namespace remains;
- the only remaining `human-in-loop` text in Codex config is the ordinary
  project trust entry for the preserved source checkout;
- LOCAL_SKILLS records the two integration components as paused with no
  discovery exposure;
- the collaboration discovery symlink still resolves to this canonical
  checkout, and the task commit has no policy-content diff from the pin;
- final active human-in-loop activation count: **0**.

No broad tests, unrelated scanners, dependency changes, environment creation,
or execution of the broken Skill were used. In accordance with the committed
task, `ask_human` and `notify_human` were not invoked, including at terminal
handoff.

## Unrelated repository preservation

Direct child Git repositories under `/Users/wenv/Documents/skills` other than
this repository and the in-scope preserved source were snapshotted by HEAD and
full porcelain-status digest before and after the machine changes. The compared
repositories were `axiomfig`, `paper_download`, `sci-manuscript`, `tikz-skill`,
and `wos-spider`. Every HEAD and status digest was identical; no unrelated
project repository changed.

## Publication

This report is the only repository change made by Codex. It is committed and
pushed on `task/260917-disable-human-in-loop`; a final fresh fetch verifies the
local task HEAD equals its upstream. The exact self-referential publication SHA
is returned in the task result contract.
