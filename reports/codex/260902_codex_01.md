# Global Codex policy and first-party Skill renames — Codex Report

VERDICT: PASS WITH LIMITATIONS
Task ID: 260902_chatgpt_01
Task source SHA: eb454c09e290bcfc8b513fe36ec2ad62869d3f02
Baseline SHA: 47005ae9eddd7d44b4c1c7c1de635050fdaec6e8
Final SHA: 9ed9d3445258c24484f1d7e13ac96009b04d89c9
Verification level: LEVEL 2 — MAJOR

## Changes actually made

- Replaced `/Users/wenv/.codex/AGENTS.md` with the exact substantive policy in
  the committed task.
- Renamed the existing public GitHub repository `cigit-zgy/axiomfig-skill` to
  `cigit-zgy/axiomfig`, updated active repository URLs and references, explicitly
  reset `origin`, and renamed the local folder to
  `/Users/wenv/Documents/skills/axiomfig`. The rename commit is
  `32749859460f56f5bd48e4cdab16d8a7786f5d62`.
- Renamed the existing public GitHub repository
  `cigit-zgy/sci-manuscript-skill` to `cigit-zgy/sci-manuscript`, updated the
  Skill name, distribution metadata and active references, explicitly reset
  `origin`, and renamed the local folder to
  `/Users/wenv/Documents/skills/sci-manuscript`. Six concurrent upstream
  KXTB-CAS migration commits were preserved by rebasing the unpushed rename
  commit onto `origin/main`; the final rename commit is
  `ec9a25d2b6ab91a14fbf51bbdfcebd44bab03e53`.
- Updated `/Users/wenv/Documents/skills/LOCAL_SKILLS.md` to use only
  `FIRST_PARTY` and `THIRD_PARTY`, record the renamed repositories, paths and
  revisions, and treat upstream repositories and plugins as THIRD_PARTY
  distribution mechanisms. Removed its duplicate global routing section after
  the new global AGENTS file became the routing authority.
- Linked `260902_chatgpt_01` in both the metadata and body of
  `reports/concept/00_global_codex_policy.md` without changing the accepted
  policy decision.
- Did not move, rewrite, reinstall or otherwise broadly re-migrate third-party
  Skills.

## Verification actually run

1. Fetched `agent-collaboration`, fast-forwarded clean `master` from
   `295a2b83f7844147d39bc1711172f15f1996cd3c` to the task commit, and verified
   baseline `47005ae9eddd7d44b4c1c7c1de635050fdaec6e8` is an ancestor of task source
   `eb454c09e290bcfc8b513fe36ec2ad62869d3f02`.
2. Extracted the approved global policy from the task commit and ran `diff -u`
   against `/Users/wenv/.codex/AGENTS.md`; exit status was 0.
3. Started a clean, ephemeral Codex 0.151.0 session in a temporary non-Git
   directory with read-only sandboxing. Without reading files or using tools,
   the session returned exactly:

   ```text
   # Global Codex operating policy
   FIRST_PARTY,THIRD_PARTY
   /Users/wenv/Documents/skills/LOCAL_SKILLS.md
   ```

4. AxiomFig affected checks: `4 passed` in
   `tests/test_release_readiness.py`, `blind_agent.py` byte-compilation, Skill
   quick validation PASS, active tracked old-name search empty, clean pushed
   `master`, exact new `origin`, GitHub identity `cigit-zgy/axiomfig`, PUBLIC
   visibility, and original pre-task commit verified as an ancestor.
5. sci-manuscript affected checks in a clean environment at the final path:
   Ruff PASS for the two touched Python modules, installed distribution metadata
   and imported `__version__` both `2.1.0`, CLI output
   `sci-manuscript 2.1.0`, `33 passed` for build/manifest focused tests, Skill
   quick validation PASS, active tracked old-name search empty, clean pushed
   `main`, exact new `origin`, GitHub identity `cigit-zgy/sci-manuscript`, PUBLIC
   visibility, and original pre-task commit verified as an ancestor.
6. Verified the only uppercase ownership tokens in `LOCAL_SKILLS.md` are
   `FIRST_PARTY` and `THIRD_PARTY`; the two old local folders are absent and the
   two new folders exist.
7. Searched active non-historical tracked content and global routing/configuration
   for `axiomfig-skill` and `sci-manuscript-skill`; no active match remained.
8. Checked every symlink below `/Users/wenv/.codex/skills`; broken symlink count
   was zero. The before/after discovery topology was unchanged by this task.
9. Ran the combined acceptance matrix with `set -e`, repository status checks,
   identity checks, GitHub checks, history checks, concept-linkage checks and
   `git diff --check`; it exited 0.
10. An independent read-only adversarial reviewer checked contradiction,
    overreach, duplicate routing/discovery and over-engineering. It found no
    Critical issue. Its duplicate-routing and stale concept-link findings were
    corrected, then confirmed resolved. It agreed the third-party regression is
    PASS only with the pre-existing discovery limitation stated below.

## Acceptance criteria

| Criterion | Result | Evidence |
|---|---|---|
| Exact global AGENTS | PASS | Byte-for-byte task extraction diff exited 0 |
| Global AGENTS discovery | PASS | Clean ephemeral session returned the exact policy title, classes and inventory path |
| Exactly two ownership classes | PASS | Unique uppercase ownership tokens are `FIRST_PARTY,THIRD_PARTY` |
| `axiomfig` rename | PASS | Folder, Skill name, GitHub name, origin, inventory and active references agree |
| `sci-manuscript` rename | PASS | Folder, Skill/distribution name, GitHub name, origin, inventory and active references agree |
| Visibility and history preserved | PASS | Both repositories remain PUBLIC; original commits are ancestors of final commits |
| Third-party discovery regression | PASS | No broken symlink or duplicate discovery was introduced by this task |
| Concept linkage | PASS | Task ID appears in concept metadata and the dated decision body |
| Codex report committed and pushed | PASS | This report is the final task artifact; its containing commit is reported in final stdout |

## Limitations

- Third-party discovery was not globally clean before this task:
  `/Users/wenv/.codex/skills/.system/openai-docs` and
  `/Users/wenv/.codex/skills/openai-docs` both declare `openai-docs`. Their birth
  dates predate this task, neither was changed here, and task section D prohibits
  broad re-migration. `LOCAL_SKILLS.md` therefore records the user-managed
  migration inventory but is not exhaustive for Codex-managed/bundled runtime
  Skills such as `.system`, `openai-docs`, `pdf` and `codex-primary-runtime`.
- The clean discovery session succeeded but emitted unrelated existing warnings
  about plugin icon paths, rollout state-database fallback and MCP shutdown.
- Historical reports and Git reflogs retain old repository names as provenance;
  they are not active routing references and were intentionally not migrated.
- One full sci-manuscript pytest invocation ran beyond the original tool-output
  window, so its final summary was not retained and is not credited as evidence.
  The affected final-path checks listed above are fresh and passed.
- Both renamed repositories remain PUBLIC. That preserves their pre-task
  visibility as required, even though the general first-party policy prefers
  PRIVATE unless explicitly chosen otherwise.

## Remaining blockers

None.

## Explicit non-goals

- No `agent-collaboration` redesign.
- No third-party Skill rewrite or broad migration.
- No historical report/reference migration.
- No repository visibility change.
- No new scripts, daemons, manifests, caches, hash registries, generators or
  compatibility layers.

## Git state

- `axiomfig`: clean `master`, equal to `origin/master` at
  `32749859460f56f5bd48e4cdab16d8a7786f5d62`.
- `sci-manuscript`: clean `main`, equal to `origin/main` at
  `ec9a25d2b6ab91a14fbf51bbdfcebd44bab03e53`.
- `agent-collaboration`: implementation commit
  `9ed9d3445258c24484f1d7e13ac96009b04d89c9` was pushed before authoring this
  report. The commit containing this report is the final repository SHA reported
  in fixed stdout.
- `/Users/wenv/.codex/AGENTS.md` and
  `/Users/wenv/Documents/skills/LOCAL_SKILLS.md` are machine-global files outside
  the `agent-collaboration` repository.
- AxiomFig's verification-created `.venv` was moved recoverably to
  `/Users/wenv/.Trash/axiomfig-venv-codex-260902`. sci-manuscript's stale
  pre-rename `.venv` was moved recoverably to
  `/Users/wenv/.Trash/sci-manuscript-venv-pre-rename-260902`, and a clean working
  environment was rebuilt at the final path.
